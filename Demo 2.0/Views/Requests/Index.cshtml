@model IEnumerable<Demo_2._0.Models.po_avise_requests>


@{
    ViewBag.Title = "Avisierungen";
}


<h2>Avisierungen</h2>
<p>Current Time @DateTime.Now</p>

<p>
   
</p>


<p>
    @Html.ActionLink("Create New", "Create")
</p>


@using (Html.BeginForm())
{
    <p>
        Find by name: @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}

<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.po_no)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zeitpunkt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Besteller)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.item_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.off_loading_advise)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Entladung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.po_no)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zeitpunkt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Besteller)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.item_description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.off_loading_advise)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entladung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.ActionLink("Enter Transportation details", "Index", "Transportations")
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.uid }) |
                @Html.ActionLink("Details", "Details", new { id = item.uid }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.uid })
            </td>
        </tr>
    }

</table>
